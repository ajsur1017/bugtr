<html>
<%- include("./partials/head.ejs") %>

    <body>

        <div class="container">
            <h1>Budgtr Index</h1>
            <table>
                <% for(let i=0; i < budget.length; i++) { %>
                    <tr>
                        <td>
                            <%= budget[i].date %>
                        </td>
                        <td><a href="/budgets/<%=i%>">
                                <%= budget[i].name %>
                            </a></td>
                        <td>
                            <%= budget[i].amount %>
                        </td>

                        <% } %>

            </table>


        </div>

    </body>

</html>